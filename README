Interoperation ----

Building BDB 1.85 apps on linux (modern linux) is annoying.
Instead, if you have a bdb 1.85 dataset, use:
	db_dump185 <old> | db_load <new bdb 4.x / 5.x>
	then
	bdb_to_leveldb <leveldb> <bdb>

And then you can use dbtransactor / ....

